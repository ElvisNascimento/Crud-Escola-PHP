<section class="mt-3 card card-body">
    <h1>Gerenciar Usuarios</h1>

    <div class="mt-2">
        <a href="/usuarios/novo" class="btn btn-outline-dark btn-sm">Novo Usuario</a>
    </div>

    <table class="table table-striped table-hover mt-4">
        <thead class="table-dark">
            <tr>
                <th>#ID</th>
                <th>Nome</th>
                <th>Email</th>
                <th>Perfil</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            <?php
                foreach ($users ?? [] as $user) {
                    echo "
                        <tr>
                            <td>{$user->id}</td>
                            <td>{$user->name}</td>
                            <td>{$user->email}</td>
                            <td>{$user->profile}</td>
                            <td>
                                <a href='/usuarios/editar?id={$user->id}' class='btn btn-outline-warning btn-sm'>Editar</a>
                                <a href='/usuarios/excluir?id={$user->id}' class='btn btn-outline-danger btn-sm'>Excluir</a>
                            </td>
                        </tr>
                    ";
                }
            ?>
        </tbody>
    </table>
</section>