<section class="mt-3 card card-body">
    <h1 class="text-center">Gerenciar Categorias</h1>

    <div class="mt-2 text-center">
        <a href="/categorias/novo" class="btn btn-outline-dark btn-sm">Nova Categoria</a>

        <a href="/categorias/relatorio" target="_blank" class="btn btn-outline-dark btn-sm">Gerar Relatorio de Categorias em PDF</a>
    </div>

    <table class="table table-striped table-hover mt-4">
        <thead class="table-dark">
            <tr>
                <th>#ID</th>
                <th>Nome</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            <?php
                // if(isset($categorias) && sizeOf($categorias) > 0){
                    foreach ($categorias ?? [] as $categoria) {
                        echo "
                            <tr>
                                <td>{$categoria->id}</td>
                                <td>{$categoria->nome}</td>
                                <td>
                                    <a href='/categorias/editar?id={$categoria->id}' class='btn btn-outline-warning btn-sm'>Editar</a>
                                    <a href='/categorias/excluir?id={$categoria->id}' class='btn btn-outline-danger btn-sm'>Excluir</a>
                                </td>
                            </tr>
                        ";
                    }
                // }
            ?>
        </tbody>
    </table>
</section>